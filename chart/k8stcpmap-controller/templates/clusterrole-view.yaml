{{- if .Values.clusterRBAC.enabled -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ template "k8stcpmap-controller.fullname" . }}-view
  labels:
    app.kubernetes.io/name: {{ include "k8stcpmap-controller.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "k8stcpmap-controller.chart" . }}
    rbac.authorization.k8s.io/aggregate-to-view: "true"
  annotations:
    {{- toYaml .Values.annotations | nindent 4 }}
rules:
- apiGroups:
  - "networking.infra.doodle.com"
  resources:
  - tcpingressmappings
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - "networking.infra.doodle.com"
  resources:
  - tcpingressmappings/status
  verbs:
  - get
{{- end }}
